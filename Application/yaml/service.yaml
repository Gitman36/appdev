kind: Service
apiVersion: v1
metadata:
# set your user name
  name: redhatuser82-hello-world
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
# set your user name
    app: redhatuser82-hello-world